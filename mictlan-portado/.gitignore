.env
__pycache__/
*.pyc
venv/
.venv/

# Modulos externos del SDK -- nunca pasan por revision de codigo del repo
# (ver CONTRATO_SDK_MODULOS.md). Incluye el almacen propio de cada modulo
# (external_modules/<id>/estado/*.db) y sus archivos de referencia
# (external_modules/<id>/datos/*).
external_modules/
# Catalogos de referencia compartidos entre modulos (CSVs/.db de solo
# lectura) -- datos operativos, no codigo.
datos_compartidos/
